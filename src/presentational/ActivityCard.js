import React from 'react'

const ActivityCard = props => {
    return (
        <div>
            I'm meant to display a single activity that google places feeds back or that a user creates for themselves. 
        </div>
    )
}